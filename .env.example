# Desarrollo: DEBUG=True, produccion/Docker: DEBUG=False
DEBUG=True

SECRET_KEY=cambia-esta-clave-secreta-en-produccion
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos (solo produccion/Docker — en desarrollo se usa SQLite)
DB_NAME=blog_db
DB_USER=blog_user
DB_PASSWORD=password-seguro-aqui
DB_ROOT_PASSWORD=root-password-seguro-aqui
DB_HOST=db
DB_PORT=3306
